pipeline {
  agent any
  stages {
    stage('') {
      steps {
        sh 'docker login -u $DOCKER_USER -p$DOCKER_PASS'
      }
    }

  }
  environment {
    DOCKER_USER = 'itsvictorfy'
    DOCKER_PASS = 'Uheyur1252@$'
  }
}